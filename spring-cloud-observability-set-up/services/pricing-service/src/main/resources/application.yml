server:
  port: 8081

spring:
  application:
    name: pricing-service

management:
  endpoints:
    web:
      exposure:
        include: health,info, metrics, prometheus
  endpoint:
    prometheus:
      enabled: true
