server:
  port: 8081

spring:
  profiles:
    active: default
  application:
    name: pricing-service

management:
  endpoints:
    web:
      exposure:
        include: health,info

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka